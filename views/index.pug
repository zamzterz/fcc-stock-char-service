doctype html5

html
	head
		title FCC Stock app
		link(rel="stylesheet" href="/public/main.css")
	body

		canvas(id="stockChart", height=400, width=600)

		div(id="addStock", class="hidden")
			label(for="add") Add stock
			input(type=text, id="stockName")
			button(type=button, id="addStockBtn") Add

		div(id="stockList")

		script(src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.3.0/Chart.min.js")
		script(src="https://cdnjs.cloudflare.com/ajax/libs/randomcolor/0.4.4/randomColor.min.js")
		script(src="/public/stockController.js")
